# ------------------------------
# SBV Second Brain - env 示例
# 拷贝为 .env 后再填入真实值（不要提交真实 Key）
# ------------------------------

# --- 必填：对话/画像（Gemini） ---
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY

# --- 可选：联网搜索（SerpAPI） ---
SERPAPI_API_KEY=YOUR_SERPAPI_API_KEY

# --- 可选：Notion 增量同步 ---
NOTION_API_KEY=
NOTION_DATABASE_ID=

# --- 可选：X/Twitter 增量同步（RapidAPI） ---
RAPIDAPI_KEY=
RAPIDAPI_HOST=
# 多个账号用逗号分隔（不要带 @）
X_USERNAMES=mjpmaa,naval

# --- 可选：Telegram Bot ---
TELEGRAM_BOT_TOKEN=
# 允许的 chat_id 白名单（逗号分隔）；留空=不限制
TG_ALLOWED_CHAT_IDS=
TG_MAX_TURNS=20
# 管理员 user_id 白名单（逗号分隔）；用于限制 /switch 等管理命令
# 安全默认：未配置时，/switch 将默认全部拒绝（避免外部用户随意更换风格）
TG_ADMIN_USER_IDS=

# --- Persona（面具/System Prompt）---
# 默认 persona 文件名：personas/default.txt（当前版本固定；未提供 TG_DEFAULT_PERSONA 环境变量）
# 运行时状态：state/tg_persona_state.json（chat_id -> persona 文件名）

# --- 可选：TG Friend Mode（只影响 TG）---
TG_FRIEND_MODE=0
TG_LOW_TH=0.25
TG_HIGH_TH=0.55
TG_MIN_HITS=3
TG_RETRIEVE_TOP_K=6
TG_RETRIEVE_MAX_SCAN=4000
TG_CORPUS_PATH=data/corpus.jsonl

# --- 可选：TG 对话旁路日志 ---
TG_SAVE_DIALOG=0
TG_SAVE_DIALOG_DEBUG=0


